fixtures:
  symlinks:
    apache: "#{source_dir}"
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "2.6.0"
  repositories:
    firewall:
      repo: "git://github.com/puppetlabs/puppetlabs-firewall"
      ref: "2.6.0"
